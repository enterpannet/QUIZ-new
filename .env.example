# Local dev → API production (ต้องมี http://localhost:5173 ใน CORS_ORIGIN บน server)
VITE_KIOSK_METRICS_ENDPOINT=https://api2.d2km.online/v1/events

# QR / NFC ชี้ production (แม้รัน bun dev บน localhost)
VITE_KIOSK_PUBLIC_ORIGIN=https://quiz.d2km.online

# Local dev → API บนเครื่อง (ถ้ารัน quiz-kiosk-api เอง)
# VITE_KIOSK_METRICS_ENDPOINT=http://localhost:8787/v1/events

# Production (GitHub Actions deploy): ตั้งเป็น Repository secret แทน
#   VITE_KIOSK_METRICS_ENDPOINT
